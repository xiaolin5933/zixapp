账务系统确认流程

后台服务
1. 生成pack_mission(Ack模块接口ack_mission)
    接收到前台发送的扫描日期(扫描日期[个人认为是确认日期更好]不能>=当前日期), 获取状态控制表中指定扫描日期的记录，如果获取到记录且状态为2生成中 或 3生成成功 或 -1无，那么停止整个过程, 返回给客户端失败；否则调用计费系统(packs接口)(在确认规则表中查找所有确认周期的终止日期正好等于扫描日期的确认规则集合)，继续以下。
    a. 如果确认规则集合是空的，确认控制表是否存在该记录(主键: 扫描日期)，不存在插入记录(-1 无), 存在那么更新记录(-1 无),返回给客户成功， 停止整个流程。
    b. 如果确认规则集合不为空，确认控制表是否存在该记录(主键: 扫描日期)，不存在插入记录(1 可生成), 存在那么更新记录(1 可生成)， 执行步骤2，返回给客户成功。
2. 创建新进程,生成0031文件(Ack模块接口ack)
    如果为1可生成
        a. 更新状态控制表为(2 生成中)。
        b. 那么按获取到的确认规则集合循环执行下面步骤
            b1  (暂估手续费科目指定确认规则按交易日期分类汇总作为输入)调用计费系统(verify接口)返回 实际总额，打折比例，确认周期
            b2  导出一个sql语句查询出来的内容到0031流水文件(指定确认规则id按各核算项分类汇总暂估手续费, 进行分摊，并且算出ark0)。 如果导出不成功，停止整个处理,将控制表状态更新为(-2 生成失败)；否则继续。
        c. 生成load_mission。
        d. 更新状态控制表为(3 生成成功)。
    否则停止



控制界面
1. 确认控制界面可以看到是否已经生成成功.
2. 确认控制界面如果是 
   可生成(1)                     => (只有生成按钮)点生成 => 调用后台服务
   生成中(2)                     => 没有按钮
   生成成功(3)                   => 没有按钮
   无(-1)                        => 没有按钮
   生成失败(-2)                  => (只有生成按钮)点生成 => 调用后台服务
